<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:paddingLeft="@dimen/fragment_base_padding"
    android:paddingRight="@dimen/fragment_base_padding"
    tools:context=".MainActivity$PlaceholderFragment">

    <WebView
        android:id="@+id/webView_rules"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <ProgressBar
        android:id="@+id/progressBar_rules"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="@dimen/progressBar_spinner_width"
        android:layout_height="@dimen/progressBar_spinner_width"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true" />

    <TextView
        android:id="@+id/textView_no_connection_rules"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:text="@string/no_connection_text"
        android:textSize="20sp" />

</RelativeLayout>
